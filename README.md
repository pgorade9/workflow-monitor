This repository has a good example of aio-http retry calls
aiohttp_retry does not need client_session (optional)
you can set your retry options
aiohttp client session allows to make asynchronous calls
